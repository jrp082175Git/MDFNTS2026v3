# Redis config overrides
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# Network overrides (Applies to both PROD and DR unless specifically overridden below)
MULTICAST_INTERFACE=0.0.0.0
MULTICAST_PORT=10001
MULTICAST_GROUP=239.1.1.1

# Specific Network overrides (Takes precedence over the global settings above)
# PROD_MULTICAST_GROUP=239.1.1.1
# PROD_MULTICAST_PORT=10001
# DR_MULTICAST_GROUP=239.2.2.2
# DR_MULTICAST_PORT=10001

# Server overrides
SOCKETIO_HOST=0.0.0.0
SOCKETIO_PORT=3000

TCP_RELAY_HOST=0.0.0.0
TCP_RELAY_PORT=4000

TCP_RETRANS_HOST=0.0.0.0
TCP_RETRANS_PORT=5000
